<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Profile</title>
    
    <% include ./partials/header.ejs %>
    <link rel="stylesheet" href="/css/profile.css">

</head>
<body>

    <% include ./partials/navbar.ejs %>

    <div class="container mt-4">
        <h2>Edit Profile</h2>
        <form action="/edit-profile" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <input type="text" class="form-control" name="name" value="<%= user.name %>" placeholder="Name" required>
            </div>
            <div class="form-group">
                <input type="text" class="form-control" name="address" value="<%= user.address %>" placeholder="Address">
            </div>
            <div class="form-group">
                Current Profile Picture
                <br>
                <% if(user.orientation=='on') { %>
                    <img class="on-image" src="<%= user.picture %>" alt="">
                <% } else if(user.orientation=='off') { %>
                    <img class="off-image" src="<%= user.picture %>" alt="">
                <% } else { %>
                    <img class="grav-image" src="<%= user.picture %>" alt="">
                <% } %>                    
                <br>    
                <% if(user.orientation!='gravatar') { %>
                    <input type="checkbox" class="my-3" name="removePic"> Remove Current Profile Picture.
                <% } %>
                <br>
                <label for="profile">New Profile Picture</label>
                <input type="file" accept="image/*" class="form-control" name="profilePic" id="profile">
                <input type="checkbox" class="my-3" name="orientation"> Tick this if image is Potrait.
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</body>
</html>
